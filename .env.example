# --- La Belle Vie account -------------------------------------------------
# Your labellevie.com login. Set these in the Vercel project's Environment
# Variables (Production + Preview), NOT in the repo. Locally, copy this file
# to .env.local (git-ignored) to run scripts/verify-auth.ts and integration tests.
LBV_EMAIL=you@example.com
LBV_PASSWORD=your-password

# --- MCP server auth ------------------------------------------------------
# Bearer token the MCP server requires from every client. Generate a long
# random string (e.g. `openssl rand -hex 32`). Give the same value to the
# agent's MCP config and to the CLI (LBV_API_TOKEN below).
LBV_API_TOKEN=generate-a-long-random-token

# --- Descope OAuth (Claude Desktop / claude.ai connectors) ----------------
# Optional second auth path alongside LBV_API_TOKEN. Both must be set for
# OAuth to be active — the server fails closed otherwise. Set them on Vercel
# (Production + Preview, Sensitive) and redeploy.
# Project ID from the Descope console (not a secret).
DESCOPE_PROJECT_ID=
# THE security boundary: only a Descope token whose email claim matches this
# address (case-insensitive) is accepted, regardless of Descope-side config.
LBV_ALLOWED_EMAIL=redacted@example.com
# Optional regional override; defaults to https://api.descope.com
# DESCOPE_BASE_URL=
# Local descope CLI only (provisioning). NEVER set on Vercel, never commit,
# never paste in chat — the server runtime does not use it.
# DESCOPE_MANAGEMENT_KEY=

# --- Session store (Vercel KV / Upstash Redis) ----------------------------
# Cookie-jar session cache + MCP SSE state. On Vercel, adding a KV/Upstash
# store injects these automatically. If absent, the server falls back to an
# in-memory store (fine for local dev, not for serverless production).
KV_REST_API_URL=
KV_REST_API_TOKEN=
# Used by mcp-handler for SSE stream resumability (a redis:// URL).
REDIS_URL=

# --- CLI ------------------------------------------------------------------
# Where the thin CLI connects. Point at the deployed server in production.
LBV_MCP_URL=http://localhost:3000/mcp
